/**
 * /calendar — Calendar view (Jalali month grid, drag-drop scheduling)
 */
import { CalendarView } from "@/components/views/calendar-view";

export default function CalendarPage() {
  return <CalendarView />;
}
