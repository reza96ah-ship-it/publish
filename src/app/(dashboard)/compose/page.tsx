/**
 * /compose — Compose view (Tiptap editor, Jalali picker, AI assistant)
 */
import { ComposeView } from '@/components/views/compose-view'

export default function ComposePage() {
  return <ComposeView />
}
